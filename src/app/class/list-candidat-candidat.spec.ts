import { ListCandidatCandidat } from './list-candidat-candidat';

describe('ListCandidatCandidat', () => {
  it('should create an instance', () => {
    expect(new ListCandidatCandidat()).toBeTruthy();
  });
});
